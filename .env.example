# 클라이언트: BFF 동일 오리진 프록시 경로
NEXT_PUBLIC_API_BASE_URL=/api

# 서버 전용 (BFF → 외부 SlidTodo API). NEXT_PUBLIC_ 접두어 금지.
SLIDTODO_API_BASE_URL=https://slid-to-do-api.vercel.app
SLIDTODO_TEAM_ID=

# 라이브 통합 테스트 전용 (로컬 .env.local 에만, 커밋 금지). 미설정 시 라이브 테스트 skip.
SLIDTODO_TEST_EMAIL=
SLIDTODO_TEST_PASSWORD=
